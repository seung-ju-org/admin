1. Access your application using the configured Service or Ingress.
2. Update values.yaml with image/tag and required environment variables.
3. For production secrets, set existingSecret and disable inline secret values.
